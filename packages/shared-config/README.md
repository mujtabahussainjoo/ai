# @myaibuddy/shared-config

Shared TS/ESLint/Prettier presets.

Usage (frontend):

```json
{ "extends": ["@myaibuddy/shared-config/tsconfig.base.json"] }
```

Flat ESLint config composes from `@myaibuddy/shared-config/eslint`; Prettier from `@myaibuddy/shared-config/prettier`.